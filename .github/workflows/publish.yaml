on:
  push:
    branches:
      - publish

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main
      - uses: DeterminateSystems/flake-checker-action@main

      - run: |
          echo git config --global user.name "${{ env.CI_COMMIT_AUTHOR }}"
          echo git config --global user.email "username@users.noreply.github.com"
          echo git remote set-url origin https://x-access-token:${{ secrets.PAT }}@github.com/${{ github.repository }}
          echo nix develop -c mvn --batch-mode release:prepare
          echo nix develop -c mvn --batch-mode release:performz
